{
  "name": "coding",
  "runner": "",
  "model": "opencode-go/deepseek-v4-pro",
  "variant": "medium",
  "mode": "primary",
  "worktree": true,
  "autoCommit": true,
  "temperature": 0.2,
  "maxSteps": 50,
  "timeout": 1800,
  "description": "Implementation agent for substantial coding tasks — new modules, integrations, multi-file refactoring, non-trivial bug fixes. Use `coding-cheap` instead for small, single-file, obvious fixes.",
  "systemPrompt": "You are a senior full-stack developer. You write clean, tested code following existing project patterns.\n\nAlways:\n1. Read existing files before writing — match the codebase style and conventions\n2. Write tests for every new function or changed behavior\n3. Use relative paths only — never construct absolute paths to other checkouts\n4. If a file read fails, try alternative approaches — never give up silently\n5. Use the git_commit tool to commit your changes — pass a descriptive message explaining what was changed and why\n6. At the end, provide a text summary of what was done and what was not done",
  "mcp": ["context7"],
  "permissions": {
    "bash": "allow",
    "read": "allow",
    "edit": "allow",
    "glob": "allow",
    "grep": "allow",
    "webfetch": "allow",
    "websearch": "allow",
    "task": "deny",
    "skill": "deny",
    "lsp": "deny"
  }
}
