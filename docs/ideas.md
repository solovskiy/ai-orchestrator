# Идеи и задачи на будущее

Накоплено по итогам тестового прогона 2026-07-27 (v3 agents).

## 🔴 Роадмап по ревью 2026-08-04 (5 агентов, публикация на GitHub)

Найдено при аудите архитектуры перед потенциальной публикацией. Порядок —
по убыванию приоритета внутри каждого блока.

### ~~Критично: агенты в других проектах исполняются по устаревшему конфигу~~ (сделано, 2026-08-05)
`opencode --agent <name>` берёт определение из `~/.config/opencode/agents/`,
а туда деплой идёт только вручную (`agent init`) или из дашборда — и
дашборд (`lib/dashboard.js:607`) пишет **только** в `AI_HOME/.opencode/agents`
(локальную копию самого `.ai`), не в глобальную папку. Проверено 2026-08-04:
`coding`/`research` в global разошлись с `agents/*.json` (старый промпт,
старая модель), `coding-cheap`/`copywriter`/`research-code` в global вообще
отсутствуют — на практике эти агенты недоступны ни в одном проекте, кроме
самого `.ai`. Модель не проседает (передаётся explicit `--model` поверх
`.md`), но systemPrompt и permissions — да.
**Фикс:** деплоить в global (или туда, где реально стоит `.ai`) при
сохранении в дашборде и/или при `agent start`/`delegate`, а не только
руками через `agent init`.
**Решено окончательно 2026-08-05** (коммит `8774da0`, задача
`dashboard-global-deploy`): `apiAgentSave` в `lib/dashboard.js` теперь сам
деплоит `.md` и локально, и в `~/.config/opencode/agents/` при каждом
сохранении через дашборд — больше не нужен ручной `agent init` после
каждой правки. Добавлен тест в `test/dashboard-api.test.js` с подменой
`HOME` на временную директорию.

### ~~Критично: тестовый прогон портит живой конфиг агента~~ (сделано, 2026-08-05)
`test/agent-read-dashboard.test.js` (тест «PUT /api/agents/research…»)
работал с боевой папкой `agents/` и реальным HTTP `PUT`, который триггерил
auto-deploy в `.opencode/agents/research.md`. JSON тест восстанавливал в
`finally`, а задеплоенный `.md` — нет. После `node --test test/*.test.js`
`research.md` оставался с `description: Research agent (тест)`,
`model: deepseek-v4-flash-changed`, промптом «Тест» и урезанными правами —
воспроизведено дважды 2026-08-04. Диагностировать на глаз было невозможно:
сам JSON цел, ломался только сгенерированный артефакт.
**Фикс** (коммит `88d030a`, задача `fix-test-corrupts-research-md`): тест
сохраняет содержимое `.opencode/agents/research.md` до `PUT` и
восстанавливает его в `finally` (или удаляет, если файла не было) — рядом
с уже существующим восстановлением JSON.

### ~~Два падающих теста~~ (сделано, 2026-08-05)
- `deploy-agent.test.js` → «freshness — второй запуск… пропускает запись»:
  freshness-check убран коммитом `40d37ee`, тест остался. **Фикс**
  (коммит `661a6fb`): кейс переписан под актуальное поведение
  (`.md` перегенерируется всегда, без freshness-check) — `lib/deploy-agent.js`
  не трогали, возврат freshness-check был бы регрессом к багу `40d37ee`.
- `agent-read-dashboard.test.js` → «variant не должен быть пустым»: падал
  из-за живого `agents/research.json`, где `variant: null` — тест был
  зашит на конкретное значение конфига, а не на инвариант. **Фикс** (тот же
  коммит `88d030a`, что и выше): ассерт теперь проверяет
  `typeof variant === 'string'` (поле присутствует, а не конкретное
  значение).
- Отдельно: `node --test test/` (без glob) падает на Node 24 —
  `MODULE_NOT_FOUND`, работает только `node --test "test/*.test.js"`. Нет
  `package.json` — стоит завести и зафиксировать команду. **Не сделано**,
  переносится отдельным пунктом.

### ~~`opencode-go/*` отсутствует в `lib/models.json`~~ (сделано, 2026-08-05)
Все 5 текущих агентов используют модели с этим префиксом. Он не был
зарегистрирован → `detectRunner()` (`lib/agent.js:52`) возвращал `null` →
оставался дефолтный `runner=opencode`, что случайно работало, но опечатка в
префиксе молча уезжала бы в opencode без явной ошибки вместо fail-fast.
Добавлена запись `"opencode-go": "opencode"` в `lib/models.json`.

### Мёртвый/устаревший код — кандидаты на удаление перед публикацией
- `hooks/` — не подключены нигде (нет `.claude/settings.json` в репо, в
  глобальном `settings.json` секции `hooks` тоже нет), плюс захардкожен
  `D:/work/vodovorot/...` и модель `deepseek/deepseek-v4-pro` — либо
  подключить и зачистить хардкод, либо выпилить.
- ~~`capabilities/`~~ — удалено (2026-08-05), v2-фолбэк больше не существует.
- `memory/` (`remember`/`recall`/`forget`/`learn`) — на деле 4 записи,
  3 из них дублируют/противоречат `agents/*.json` (`model:default` не
  совпадает с реальными моделями агентов). Оценить, нужна ли вообще
  отдельная подсистема памяти, или выпилить в пользу `agents/*.json` как
  единственного источника правды.
- `chat` — блокирующий foreground-режим (результат не в `jobs/`, сессия не
  продолжается через `send`) — противоречит центральному тезису проекта
  «оркестратор не блокируется».
- раннер `codex` — README сам признаёт «parser mismatch, Codex не отдаёт
  JSON». Для публичного репо это невыполненное обещание в таблице
  раннеров — пометить явно experimental/broken или убрать из таблицы до
  починки.

### ~~Документация разошлась с кодом (5 агентов, а не 2)~~ (сделано, 2026-08-05)
- README «Capability / Agent» и «Мультимодельность» — таблицы под 2
  агента и без `opencode-go/*`. → таблица агентов заменена на команду
  (`agent agent list`/`show`/`create`), не хардкодится; строка
  `opencode-go/*` добавлена.
- `.claude/skills/agent-bridge/SKILL.md` — только `--capability`,
  агентов `coding-cheap`/`copywriter`/`research-code` не знает. → переписан по
  решению ниже (routing через `agent agent list`, без перечисления имён),
  переустановлен в `~/.claude/skills/agent-bridge/`.
- `docs/workflow.md` — таблица под research/coding, дефолт модели не
  совпадает с фактическим набором моделей агентов. → таблица переведена
  на архетипы (research-тип/coding-тип) + явная нота про `--repo` и
  git-репозиторий для worktree-агентов.
- `docs/ideas.md` (этот файл, старые секции ниже) — часть пунктов уже
  смерджена (пагинация, kill/delete, byCapability→agent, авто-деплой из
  дашборда), но помечена как открытая — свести at some point. (не тронуто
  в этом заходе, отдельная задача)
- README «agent init» описан то как копирование SKILL.md, то как
  генерация `.md` в `~/.config/opencode/agents/` в разных местах — фактически
  делает и то, и другое, и деплоит плагины; свести в одно консистентное
  описание. (не тронуто в этом заходе)

**Решение по разбиению `agent-bridge` на 5 скиллов (обсуждено, не делать):**
routing уже есть — в `description` каждого агента (`agent agent list`),
там уже написано «Use `coding-cheap` instead for…». Пять похожих скиллов
будут конкурировать по description и путать выбор. Вместо разбиения —
переписать `SKILL.md` так, чтобы он не перечислял агентов пофамильно, а
инструктировал смотреть `agent agent list` — тогда шестой агент не
потребует правки скилла. **Сделано** (см. пункт выше).

### Перед публикацией на GitHub
1. **Ключи в `.claude/settings.local.json`** — два живых ключа OpenRouter
   (`sk-or-v1-…`) в открытом виде. Не в git-истории (спасает personal
   `~/.config/git/ignore`, не репозиторный `.gitignore`) — но правило
   `**/.claude/settings.local.json` нужно продублировать в `.ai/.gitignore`,
   иначе у любого, кто склонирует и сделает `git add -A`, ключи утекут.
   Ключи желательно перевыпустить на всякий случай.
2. ~~Захардкоженный `D:/work/vodovorot` в `CLAUDE.md`, `docs/workflow.md`,
   `hooks/prefer-ai-agent-over-subagent.js`.~~ (сделано, 2026-08-05) —
   `SKILL.md` теперь шаблонный (`{{AI_HOME}}`, подставляется в `cmd_init`
   при деплое через `sed`), `docs/workflow.md`/`CLAUDE.md` используют
   плейсхолдер `<путь-к-.ai>` вместо личного пути, хук сам резолвит свой
   `AI_HOME` через `path.resolve(__dirname, '..')`. Добавлен раздел
   «Установка» в README.md с моделью «один global-клон на машину,
   `--repo` таргетит любой проект» — уже так фактически и использовалось,
   теперь задокументировано и воспроизводимо для внешнего пользователя.
   Ещё не сделано: авто-редеплой skill/агентов при правке `agents/*.json`
   (см. пункт «агенты в других проектах исполняются по устаревшему
   конфигу» выше — тот же корень, чинится в фоне 2026-08-05).
   **Решено не делать:** кросс-платформенность (не-Windows) — сознательное
   решение, инструмент остаётся Windows/Git Bash-only, не в роадмапе.
3. ~~Нет `LICENSE`, `package.json`, CI.~~ (сделано, 2026-08-05, `2de1e45`) —
   MIT, `package.json` (`test` script), `.github/workflows/test.yml` на
   `windows-latest`.
4. ~~`research/binotel-chat-alternatives.md` — доменное исследование по
   телефонии заказчика, не относится к инструменту, не должно попадать в
   публичный репозиторий.~~ (сделано, 2026-08-05) — **вся** папка
   `research/` (8 отчётов + `run.log`) вынесена из `.ai` в
   `D:/work/vodovorot/archive/ai-research-legacy/` (вне репозитория, не
   потеряна) и удалена из git (`git rm -r research/`). Часть отчётов была
   про сам инструмент (не доменная), но проще и honest вынести всё разом,
   чем сортировать вручную — конвенция сохранения новых отчётов сменилась
   на `ai-research/` **в корне целевого `--repo`**, не в `.ai` (см.
   `README.md`, «Куда research-агенты кладут отчёт»), так что `research/`
   внутри `.ai` больше не нужна и не будет пересоздаваться сама.
   Ссылки на файлы бывшей `research/` в `docs/workflow.md`/`README.md`
   поправлены.
5. ~~Вся документация на русском — для аудитории нужен английский README~~
   (сделано, 2026-08-05, `962ad20`) — `README.md` теперь английский, русский
   оригинал сохранён как `README.ru.md`. Остальные `.md` (`CLAUDE.md`,
   `docs/*`) намеренно оставлены на русском — внутренние рабочие
   документы, не входная точка для внешнего читателя.

**Чек-лист публикации закрыт полностью** (2026-08-05): секреты защищены
`.gitignore`, пути портируемы, LICENSE/package.json/CI на месте,
`capabilities/`/устаревшие ссылки на `research/` вычищены, README —
англоязычный вход + `README.ru.md`. Остаётся на усмотрение перед реальным
`git push`/публикацией репозитория публично: перевыпустить ключи
OpenRouter (пункт 1 выше — это ручное действие в аккаунте, не код).

## ~~⚠️ Тесты (критично)~~ (сделано, `3bd99a6`/`42fcfac`/`1ce38e7`)
`test/deploy-agent.test.js`, `test/agent-crud.test.js`, `test/dashboard-api.test.js`
написаны — 88 тестов, 0 провалов (см. `README.md`, «Хуки и тесты»).

## 🔶 Dashboard (удобство)

### Пагинация в таблице задач
Сейчас 41 задача — таблица растёт. Добавить `?limit=50&offset=0` в API и
кнопки «← newer / older →» в UI. На сервере: параметры запроса, на клиенте:
состояние страницы.

### Кнопка «Удалить задачу» в дашборде
Сейчас удаление — только через CLI (`agent clean`). Добавить кнопку в detail-панели
и подтверждение. API: `DELETE /api/jobs/:id`.

### Кнопка «Убить задачу» (kill) в дашборде
Нельзя остановить зависшую задачу из UI. API: `POST /api/jobs/:id/kill`.

### Статистика по agent вместо worktree-прокси
`byCapability` в `/api/stats` определяет тип через `j.worktree` (coding/research).
С v3 в job.json есть поле `agent` — переключить на него. Позволит считать
статистику по каждому агенту отдельно, а не только по двум категориям.

### ~~verifyStatus для research — показывать «не требуется»~~ (сделано, `47834a4`/`66fbba4`)

### Авто-деплой агента при сохранении в дашборде
Сейчас Save → только JSON. Добавить вызов `deploy-agent.js` после сохранения,
чтобы `.opencode/agents/<name>.md` обновлялся сразу.

## 🕵️ Проверить, когда всплывёт (код-ревью 2026-07-27, не подтверждено живым прогоном)

### `agent send` — лимит параллельных задач не проверен под реальной гонкой
`cmd_send()` в `bin/agent` теперь делает ту же проверку
`running_count >= max_parallel_limit`, что и `cmd_start()` (см. коммит
`review-fixes`), но живьём с реальным превышением лимита не гонялось —
только по коду, по аналогии с уже проверенным `cmd_start()`. Если увидишь
больше N одновременно `running`-задач, продолженных через `agent send`
(не `start`/`delegate`), — смотри сюда в первую очередь: либо гонка между
проверкой и `mkdir -p "$job_dir"` (TOCTOU, тот же паттерн, что и в
`cmd_start`, там тоже теоретически возможен), либо сама проверка не
сработала.

### `lib/runners/claude.js` — флаг `--worktree` убран, не протестировано
Раньше `buildArgs()` слал `claude` CLI несуществующий флаг `--worktree
<path>` (рабочая директория и так задаётся через `cd` в `run-job.sh`).
Блок убран, но в проекте ни разу не было ни одной задачи через
`claude`-раннер, и самого `claude` CLI под рукой для проверки не было —
фикс подтверждён только чтением кода. Не критично прямо сейчас (остальные
раннеры кроме opencode — дело далёкого будущего), но если/когда дойдут
руки до `claude`/`gemini`/`codex`-раннеров — начать ревью оттуда.

## 🔹 Мелкие оптимизации

### ~~Системный промпт coding — упомянуть авто-коммит~~ (сделано, `d5f3fc4`)
Решено иначе, чем предполагалось: вместо упоминания auto-commit обёрткой
агенту дан инструмент `git_commit` (`plugins/index.js`) — коммитит сам,
явным шагом 5 в systemPrompt (`agents/coding.json`).

### ~~Deploy-agent — проверка freshness~~ (сделано, `a64061c`)
mtime `.json` vs `.md`, пропуск записи если актуально.

### ~~Путаница bin/agent vs .ai/bin/agent~~ (решено иначе, `266fb38`)
Автоопределение по `$PWD` не делали — при разборе выяснилось, что сам
скрипт резолвит свой `AI_HOME` корректно через `BASH_SOURCE[0]`
независимо от способа вызова, проблема была не в скрипте, а в том, что
bash не находит файл ДО того, как скрипт получает управление (неверный
путь под текущий cwd). Решение: зафиксировано правило в
`docs/workflow.md` (ALWAYS) — вызывать CLI абсолютным путём, это
убирает зависимость от cwd полностью.

## 💡 Крупные фичи (на подумать)

### Вкладка «Memory» в дашборде
Просмотр и редактирование `memory/index.json` через UI. Сейчас только CLI
(`remember`/`recall`/`forget`).

### Health dashboard — мониторинг агентов
График успешности по дням, топ ошибок, среднее время выполнения.
Сейчас `/api/stats` даёт сырые агрегаты — можно визуализировать.

### WebSocket вместо polling
Сейчас дашборд опрашивает `/api/jobs` каждые 5 секунд. WebSocket дал бы
мгновенные обновления. Но требует переработки сервера (сейчас чистый HTTP).

### ACP-адаптер (исследовать позже)
Agent Client Protocol мог бы стать одним адаптером на всех исполнителей.
Сейчас отложено намеренно (разбор был в `research/acp.md` — папка вынесена
из репозитория 2026-08-05, см. «Перед публикацией на GitHub» выше, пункт
про `research/binotel-chat-alternatives.md`).

---

*Последнее обновление: 2026-07-29*
